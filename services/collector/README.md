# Collector service

Reserved for the optional policy-gated, read-only public-source collector. It is disabled by default, is not required for the finale, and must not be implemented before the core plans are stable.
